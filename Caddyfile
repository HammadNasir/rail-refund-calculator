{
  admin off
  auto_https off
}

:{$PORT:3000} {
  root * /app/dist
  encode gzip
  
  # Serve static files
  file_server
  
  # For all other routes, serve index.html (SPA routing)
  try_files {path} /index.html
}